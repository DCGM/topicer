topicer:
  cls: CrossBertTopicer
  config:
    model: UWB-AIR/Czert-B-base-cased
    threshold: 0.5
    device: cuda
    max_length: 512
    gap_tolerance: 0
    normalize_score: true
    soft_max_score: false