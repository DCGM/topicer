topicer:
  cls: CrossBertTopicer
  config:
    model: ./traced-models/mmbert-best
    threshold: 0.32
    device: cuda
    max_length: 512
    gap_tolerance: 0
    stride: 32
    normalize_score: true
    soft_max_score: true
    confidence_mode: min
